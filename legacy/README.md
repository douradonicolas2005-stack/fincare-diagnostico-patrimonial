# Versão legada preservada

A versão original continua preservada na raiz do projeto para comparação:

- `fincare-diagnostico-patrimonial-inteligente.html` — simulador original;
- `index.html` — entrada estática original;
- `landing.html` — landing page original;
- `privacidade.html` — política original;
- `google-sheets-integration.gs` — integração original.

A nova aplicação Next.js fica nas pastas `app/`, `components/` e `lib/` e não sobrescreve esses arquivos.
